name: Trigger auto deployment for miabetodo0707bf9083fd

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/miabetodo0707bf9083fd-AutoDeployTrigger-c36e66f3-2f6a-4291-ab5c-180e54f15a13.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.MIABETODO0707BF9083FD_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.MIABETODO0707BF9083FD_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.MIABETODO0707BF9083FD_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.MIABETODO0707BF9083FD_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.MIABETODO0707BF9083FD_REGISTRY_PASSWORD }}
          containerAppName: miabetodo0707bf9083fd
          resourceGroup: miabetodo0707bf90
          imageToBuild: default/miabetodo0707bf9083fd:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


