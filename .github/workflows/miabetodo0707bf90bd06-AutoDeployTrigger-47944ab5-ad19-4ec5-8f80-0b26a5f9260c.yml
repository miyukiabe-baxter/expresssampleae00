name: Trigger auto deployment for miabetodo0707bf90bd06

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/miabetodo0707bf90bd06-AutoDeployTrigger-47944ab5-ad19-4ec5-8f80-0b26a5f9260c.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.MIABETODO0707BF90BD06_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.MIABETODO0707BF90BD06_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.MIABETODO0707BF90BD06_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.MIABETODO0707BF90BD06_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.MIABETODO0707BF90BD06_REGISTRY_PASSWORD }}
          containerAppName: miabetodo0707bf90bd06
          resourceGroup: miabetodo0707bf90
          imageToBuild: default/miabetodo0707bf90bd06:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


